spring:
  application:
    name: code-server
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://10.165.45.245:3306/qrcode?serverTimezone=UTC&useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: Hlxd@123456
    configuration:
      MapUnderscoreToCamelCase: true
    dbcp2:
      min-idle: 20
      initial-size: 10
      max-total: 100
      max-wait-millis: -1

mybatis:
  mapper-locations: classpath:/mapper/*Mapper.xml
  typeAliasesPackage: com.hlxd.microcloud.vo

server:
  port: 8999
  connection-timeout: 60000

eureka: # 一定要编写一个eureka的地址
  client: # 目的是为了让客户端可以通过eureka找到SpringCloudConfig微服务
    service-url:
      defaultZone: http://admin:root@127.0.0.1:9001/eureka/
  instance:
    instance-id: microdept.com
    prefer-ip-address: true
info:
  app.name: www.microdept.com
  company.name: hlxd
  build.groupId: $project.groupId$
  build.artifactId: $project.artifactId$
  app.version: V33.3.3
logging:
  level:
    com.hlxd.microcloud.dao : debug
security:
  basic:
    enabled: false

